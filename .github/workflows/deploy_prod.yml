name: deploy_prod

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

jobs:
  deploy:
    uses: ./.github/workflows/terraform_apply.yml
    secrets: inherit
    with:
      environment: prod
      working_directory: environments/prod

